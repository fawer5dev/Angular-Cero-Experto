# Introductory TypeScript Exercises

To run the program, follow these steps:

1. Clone or download the project.
2. Run `npm install` in the project folder.
3. In `main.ts`, change the path to the exercise you want to run.
4. Run `npm run dev` to start the program.
